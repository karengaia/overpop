
# Do this manually:
# TRUNCATE TABLE ml_users_bak; INSERT ml_users_bak SELECT * FROM ml_users;
# TRUNCATE TABLE ml_subscriptions_bak; INSERT ml_subscriptions_bak SELECT * FROM ml_subscriptions;

# export, import ml_subscriptions, ml-users, ml_lists

# Use users.pl as a template

# Don't forget current contents of mail-list/xsubscribers.txt